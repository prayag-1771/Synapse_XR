{
  "websocket_events": {
    "hand:data": { "direction": "Expert → Server → Worker", "payload": "Hand landmark packet" },
    "gesture:detected": { "direction": "Expert → Server → Worker", "payload": "Gesture event" },
    "voice:transcript": { "direction": "Expert → Server → Worker", "payload": "Voice subtitle" },
    "annotation:update": { "direction": "Expert → Server → Worker", "payload": "Annotation data" },
    "ai:detection": { "direction": "Worker → Server → Expert", "payload": "YOLO + Gemini results" },
    "session:join": { "direction": "Both → Server", "payload": "Session ID + user info" },
    "session:end": { "direction": "Either → Server → Both", "payload": "Session ended" },
    "webrtc:offer": { "direction": "Expert ↔ Server ↔ Worker", "payload": "WebRTC SDP offer" },
    "webrtc:answer": { "direction": "Worker ↔ Server ↔ Expert", "payload": "WebRTC SDP answer" },
    "webrtc:ice": { "direction": "Both ↔ Server", "payload": "ICE candidates" }
  }
}
